package com.druvu.excel;

/** Horizontal alignment of a cell. */
public enum Align {
    /** Excel decides: numbers and dates to the right, text to the left. */
    AUTO,
    LEFT,
    CENTER,
    RIGHT
}
